* Make sure you have sha*sum or md5sum installed
* Make hashboot.sh executable
* Place hashboot.sh anywhere in $PATH
* Install the appropriate init script
* If applicable, copy kernel-hooks.debian to /etc/kernel/post{inst,rm}.d/z-hashboot (make sure it is called after initramfs-tools)
